@extends('layouts.app')

@section('title', 'Tableau de Bord Admin - MFPT')

@section('content')
<div class="dashboard-container">
    <!-- En-tête amélioré avec fil d'Ariane -->
    <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
        <h1 class="h4 mb-0">
            <i class="bi bi-speedometer2 me-2" aria-hidden="true"></i>
            Tableau de Bord
            <span class="badge bg-primary ms-2 fs-6">Admin</span>
        </h1>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0">
                <li class="breadcrumb-item"><a href="{{ route('dashboard') }}">Accueil</a></li>
                <li class="breadcrumb-item active" aria-current="page">Admin</li>
            </ol>
        </nav>
    </div>

    <!-- Statistiques - Version améliorée -->
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4 mb-4 align-items-stretch">
        <!-- Carte Total Agents -->
        <div class="col">
            <div class="card h-100 shadow-sm border-0 hover-effect">
                <div class="card-body p-3 p-md-4">
                    <div class="d-flex justify-content-between">
                        <div>
                            <p class="text-muted mb-2 fw-semibold small">Total Agents</p>
                            <h2 class="mb-0">{{ number_format($total) }}</h2>
                        </div>
                        <i class="bi bi-people-fill fs-2 text-primary opacity-25" aria-hidden="true"></i>
                    </div>
                    <div class="mt-3">
                        <span class="badge bg-{{ $stats['evolution_total'] > 0 ? 'success' : 'danger' }}-subtle text-{{ $stats['evolution_total'] > 0 ? 'success' : 'danger' }}">
                            <i class="bi bi-arrow-{{ $stats['evolution_total'] > 0 ? 'up' : 'down' }}"></i>
                            {{ abs($stats['evolution_total']) }}%
                        </span>
                        <span class="text-muted ms-2 small">vs mois dernier</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Carte En Attente -->
        <div class="col">
            <div class="card h-100 shadow-sm border-0 hover-effect">
                <div class="card-body p-3 p-md-4">
                    <div class="d-flex justify-content-between">
                        <div>
                            <p class="text-muted mb-2 fw-semibold small">En Attente</p>
                            <h2 class="mb-0">{{ number_format($en_attente) }}</h2>
                        </div>
                        <i class="bi bi-hourglass-split fs-2 text-warning opacity-25" aria-hidden="true"></i>
                    </div>
                    <div class="mt-3">
                        <span class="badge bg-{{ $stats['evolution_attente'] > 0 ? 'success' : 'danger' }}-subtle text-{{ $stats['evolution_attente'] > 0 ? 'success' : 'danger' }}">
                            <i class="bi bi-arrow-{{ $stats['evolution_attente'] > 0 ? 'up' : 'down' }}"></i>
                            {{ abs($stats['evolution_attente']) }}%
                        </span>
                        <span class="text-muted ms-2 small">vs mois dernier</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Carte Validées -->
        <div class="col">
            <div class="card h-100 shadow-sm border-0 hover-effect">
                <div class="card-body p-3 p-md-4">
                    <div class="d-flex justify-content-between">
                        <div>
                            <p class="text-muted mb-2 fw-semibold small">Validées</p>
                            <h2 class="mb-0">{{ number_format($validees) }}</h2>
                        </div>
                        <i class="bi bi-check-circle-fill fs-2 text-success opacity-25" aria-hidden="true"></i>
                    </div>
                    <div class="mt-3">
                        <span class="badge bg-{{ $stats['evolution_validees'] > 0 ? 'success' : 'danger' }}-subtle text-{{ $stats['evolution_validees'] > 0 ? 'success' : 'danger' }}">
                            <i class="bi bi-arrow-{{ $stats['evolution_validees'] > 0 ? 'up' : 'down' }}"></i>
                            {{ abs($stats['evolution_validees']) }}%
                        </span>
                        <span class="text-muted ms-2 small">vs mois dernier</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Carte Rejetées -->
        <div class="col">
            <div class="card h-100 shadow-sm border-0 hover-effect">
                <div class="card-body p-3 p-md-4">
                    <div class="d-flex justify-content-between">
                        <div>
                            <p class="text-muted mb-2 fw-semibold small">Rejetées</p>
                            <h2 class="mb-0">{{ number_format($rejetees) }}</h2>
                        </div>
                        <i class="bi bi-x-circle-fill fs-2 text-danger opacity-25" aria-hidden="true"></i>
                    </div>
                    <div class="mt-3">
                        <span class="badge bg-{{ $stats['evolution_rejetees'] > 0 ? 'success' : 'danger' }}-subtle text-{{ $stats['evolution_rejetees'] > 0 ? 'success' : 'danger' }}">
                            <i class="bi bi-arrow-{{ $stats['evolution_rejetees'] > 0 ? 'up' : 'down' }}"></i>
                            {{ abs($stats['evolution_rejetees']) }}%
                        </span>
                        <span class="text-muted ms-2 small">vs mois dernier</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Actions Rapides - Version optimisée -->
    <div class="card shadow-sm mb-4 border-0">
        <div class="card-body p-4">
            <h2 class="h5 mb-4">
                <i class="bi bi-lightning-fill text-warning me-2" aria-hidden="true"></i>
                Actions Rapides
            </h2>
            <div class="d-flex flex-wrap gap-3">
                <a href="{{ route('agents.create') }}" class="btn btn-outline-primary px-4 py-2 d-flex align-items-center action-btn">
                    <i class="bi bi-person-plus me-2" aria-hidden="true"></i>
                    <span>Nouvel Agent</span>
                </a>
                <a href="{{ route('photos.index') }}" class="btn btn-outline-secondary px-4 py-2 d-flex align-items-center action-btn">
                    <i class="bi bi-camera me-2" aria-hidden="true"></i>
                    <span>Valider Photos</span>
                </a>
                <a href="{{ route('lots.generer') }}" class="btn btn-outline-success px-4 py-2 d-flex align-items-center action-btn">
                    <i class="bi bi-files me-2" aria-hidden="true"></i>
                    <span>Générer Lot</span>
                </a>
                <a href="{{ route('ias.create') }}" class="btn btn-outline-info px-4 py-2 d-flex align-items-center action-btn">
                    <i class="bi bi-robot me-2" aria-hidden="true"></i>
                    <span>Nouvelle IA</span>
                </a>
                <a href="#" class="btn btn-outline-dark px-4 py-2 d-flex align-items-center action-btn">
                    <i class="bi bi-graph-up me-2" aria-hidden="true"></i>
                    <span>Rapports</span>
                </a>
            </div>
        </div>
    </div>

    <!-- Section Activité Récente (Optionnelle) -->
    <div class="card shadow-sm border-0">
        <div class="card-body p-4">
            <h2 class="h5 mb-4">
                <i class="bi bi-clock-history text-primary me-2" aria-hidden="true"></i>
                Activité Récente
            </h2>
            <div class="list-group list-group-flush">
                @forelse($recent_activities as $activity)
                <div class="list-group-item border-0 px-0 py-3">
                    <div class="d-flex justify-content-between">
                        <div>
                            <span class="fw-semibold">{{ $activity->description }}</span>
                            <span class="text-muted small ms-2">par {{ $activity->causer->name ?? 'Système' }}</span>
                        </div>
                        <span class="text-muted small">{{ $activity->created_at->diffForHumans() }}</span>
                    </div>
                </div>
                @empty
                <div class="text-center py-4 text-muted">
                    Aucune activité récente
                </div>
                @endforelse
            </div>
        </div>
    </div>
</div>
@endsection

@section('styles')
<style>
    .hover-effect {
        transition: transform 0.2s, box-shadow 0.2s;
        border-left: 4px solid transparent;
    }
    .hover-effect:hover {
        transform: translateY(-3px);
        box-shadow: 0 6px 12px rgba(0,0,0,0.1) !important;
        border-left-color: var(--bs-primary);
    }

    .action-btn {
        transition: all 0.2s;
        min-width: 180px;
        justify-content: center;
    }
    .action-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.05);
    }

    .text-muted {
        color: #5c636a !important;
    }

    @media (max-width: 768px) {
        .action-btn {
            min-width: 100%;
        }
    }
</style>
@endsection
